deployment:
  staging:
    branch: master
    heroku:
      appname: born-in-makuhari-kanban
dependencies:
  "grunt-cli": *
  grunt: 0.4.x
  pre:
    - bundle install
    - npm install
  post:
    - grunt build
